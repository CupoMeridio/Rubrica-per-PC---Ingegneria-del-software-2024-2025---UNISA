/**
 * @package it.unisa.diem.team02.contactsbook.database
 * 
 * Descrizione del package:
 * Questo package contiene la classe `Database`, che gestisce la connessione al database e le operazioni 
 * di accesso ai dati per l'applicazione ContactsBook. La classe `Database` è responsabile per l'interazione 
 * con il sistema di gestione del database, inclusa la creazione, lettura, aggiornamento e cancellazione 
 * (operazioni CRUD) dei dati relativi ai contatti, agli utenti e ad altre entità dell'applicazione.
 * 
 * La classe fornisce metodi per eseguire query SQL, gestire la connessione al database e garantire 
 * che le operazioni sui dati vengano eseguite in modo sicuro ed efficiente. La classe è progettata per 
 * astrarre i dettagli della gestione del database, fornendo un'interfaccia semplice per le altre 
 * componenti dell'applicazione.
 * 
 * Classi principali:
 * - Database: Gestisce la connessione al database e le operazioni CRUD. Si occupa di stabilire la connessione, 
 *   eseguire query, recuperare e aggiornare i dati, e gestire le transazioni.
 */
package it.unisa.diem.team02.contactsbook.database;
