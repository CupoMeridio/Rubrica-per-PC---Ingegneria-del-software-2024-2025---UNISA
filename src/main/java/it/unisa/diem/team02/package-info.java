/**
 * @package it.unisa.diem.team02
 * 
 * Descrizione del package:
 * Questo package contiene la classe principale `App` dell'applicazione JavaFX. La classe è responsabile per
 * l'avvio dell'interfaccia utente, la gestione della finestra principale dell'applicazione e il caricamento
 * delle viste definite in file FXML. La classe configura la scena iniziale e gestisce il passaggio tra le diverse
 * schermate dell'applicazione.
 * 
 * La classe `App` si occupa di configurare il titolo della finestra, l'icona, la modalità a schermo intero
 * e di caricare le risorse FXML per le diverse schermate dell'applicazione.
 * 
 * Classi principali:
 * - App: La classe principale che avvia l'applicazione, gestisce la finestra principale e carica i layout
 *   definiti nei file FXML.
 * 
 * L'applicazione JavaFX gestisce l'interfaccia utente in modo dinamico, permettendo di passare da una vista all'altra
 * caricando diversi file FXML in base alle necessità.
 */
package it.unisa.diem.team02;